---
name: ga4
description: Query Google Analytics 4 and monitor A/B experiments through the `ga4` CLI (exploration segments, saved segments, dimensions, RPV, conversion, saved queries). Use when the user asks to look up GA4 numbers, compare experiment arms (control vs treatment), check RPV/CVR/revenue, pull data from a GA4 exploration, or monitor experiment underperformance.
allowed-tools: Bash(ga4:*)
hidden: true
---

# ga4 — GA4 analytics CLI

This is a discovery stub, not the usage guide. Before running any `ga4`
command, load the actual workflow content from the CLI:

```bash
ga4 skills get core             # workflows, common patterns, gotchas
ga4 skills get core --full      # include the full command reference
```

The CLI serves skill content that always matches the installed version, so
instructions never go stale. `ga4 skills list` shows everything available.
